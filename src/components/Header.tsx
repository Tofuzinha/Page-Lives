import { LogoBold } from "./LogoBold";

export function Header () {
    return (
        <header className="w-full h-[5rem] flex items-center justify-center bg-pink-500 border-b border-pink-600 ">
            <LogoBold />
        </header>

    )
};

export default Header;